name = "navegador-social-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Secretos: agrégalos con `wrangler secret put GROQ_API_KEY`
# O en el dashboard de Cloudflare → Workers → Settings → Variables
# Desarrollo local: crea un archivo .dev.vars (ver .dev.vars.example)
